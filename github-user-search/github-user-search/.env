VITE_APP_GITHUB_API_KEY
import.meta.env.VITE_APP_GITHUB_API_KEY                                        import { BrowserRouter as Router, Routes, Route, Link } from "react-router-dom";
import RepoList from "./components/RepoList";

function Home() {
  return <h2>Welcome to the GitHub Explorer 🚀</h2>;
}

function About() {
  return <h2>About this App: Built with React + Vite</h2>;
}

function App() {
  return (
    <Router>
      <div style={{ padding: "20px" }}>
        {/* ✅ Navigation */}
        <nav style={{ marginBottom: "20px" }}>
          <Link to="/" style={{ marginRight: "15px" }}>Home</Link>
          <Link to="/repos" style={{ marginRight: "15px" }}>Repositories</Link>
          <Link to="/about">About</Link>
        </nav>

        {/* ✅ Routing */}
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/repos" element={<RepoList username="octocat" />} />
          <Route path="/about" element={<About />} />
        </Routes>
      </div>
    </Router>
  );
}

export default App;